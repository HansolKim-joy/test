selectRelist = select rownum, board_no, user_id, board_title, board_views, board_date, recruit_head from (select board_no, user_id, board_title, board_views, board_date, recruit_head from tb_board join tb_recruit on (board_no = recruit_no) order by board_date desc) where rownum <= 5